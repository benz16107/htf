apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-typescript-app
  labels:
    app: my-typescript-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-typescript-app
  template:
    metadata:
      labels:
        app: my-typescript-app
    spec:
      containers:
      - name: my-typescript-app
        image: my-typescript-app:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: "3000"