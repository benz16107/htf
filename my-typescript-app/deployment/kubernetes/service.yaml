apiVersion: v1
kind: Service
metadata:
  name: my-typescript-app
  labels:
    app: my-typescript-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: my-typescript-app